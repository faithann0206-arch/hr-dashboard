[build]
  command = "rm -rf node_modules && npm install && npm run build"
  publish = "dist/public"

[build.environment]
  BASE_PATH = "/"
  PORT = "3000"
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
