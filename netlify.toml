[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  BASE_PATH = "/"
  PORT = "3000"
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
